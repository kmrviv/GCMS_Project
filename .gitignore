# --- Virtual environment ---
venv/
__pycache__/
*.pyc

# --- Licensed NIST data — DO NOT COMMIT (commercial license) ---
data/raw/
data/processed/NISTds.jsonl
data/processed/*.msp
*.msp

# --- Generated databases ---
*.duckdb
*.duckdb.wal

# --- Generated outputs (regenerated by running the pipeline) ---
outputs/
visuals/
final/
data/processed/per-file-json/
data/processed/*.jsonl
src/per-file-json/
src/sanitycheckreport

# --- Large generated reports (keep the small text ones manually if wanted) ---
reports/compare_report.txt
reports/compare_results.csv

# --- Sample input PDFs ---
# The full sample set (4.6 GB, rendered from the licensed NIST library) is NOT
# committed. Only the small, genuinely non-reproducible IISc real-instrument
# data is kept (see README).
data/samples/*
!data/samples/IISc_Data/

# --- LaTeX build artifacts (paper is reproducible from draft.ltx + references.bib) ---
*.aux
*.log
*.out
*.blg
*.synctex.gz
*.toc
*.fls
*.fdb_latexmk

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.vscode/
.claude/
